import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Overview — Daniel Péger",
};

// Deep-link entry. UI lives in PortfolioApp.
export default function OverviewPage() {
  return null;
}
